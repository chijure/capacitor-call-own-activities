import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reateguisolisjm.callownactivities',
  appName: 'call-own-activities',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
